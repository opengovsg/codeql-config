{
  "_comment": "Floors and expectations for the deployment gate; see gate/README.md. The scan matrix is derived from codeql-config.yml's packs: keys via languageMap, so a new pack language fails the gate until it gains a block here. languageMap lists only languages that build with build-mode: none.",
  "languageMap": {
    "javascript": "javascript-typescript",
    "actions": "actions"
  },
  "languages": {
    "javascript-typescript": {
      "queryCountFloor": 87,
      "expectedRuleIds": [
        "javascript/improper-domain-validation",
        "javascript/no-raw-interpolation",
        "javascript/no-root-cdn",
        "nestjs/exception-filter-crash",
        "nextjs/open-redirect",
        "react/unsafe-dom-manipulation",
        "react/unsafe-react-components"
      ]
    },
    "actions": {
      "_comment": "Floor 19 = 16 built-in + 3 from actions-custom-queries. The built-in suite resolves 17, but this config's query-filters exclude actions/missing-workflow-permissions, leaving 16 \u2014 do not 'fix' that 16 back to 17.",
      "queryCountFloor": 19,
      "expectedRuleIds": [
        "actions/unfrozen-package-install",
        "actions/unpinned-global-install",
        "actions/unvetted-package-exec"
      ]
    }
  }
}
